type Company
@model
@auth(
  rules: [
    {
      allow: owner
      ownerField: "owner"
      operations: [create, update, delete]
    }
  ]
) {
  id: ID!
  name: String!
  description: String!
  rating: Int!
  logoUrl: String!
  referencesUrl: [String]!
}

